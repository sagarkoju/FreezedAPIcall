import 'package:flutter/material.dart';

class TinderSwiperScreen extends StatelessWidget {
  const TinderSwiperScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
